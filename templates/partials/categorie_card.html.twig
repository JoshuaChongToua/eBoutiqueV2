<style>
    .prod-img {
        background-position: center;
    }

    .prod-front {
        display: flex;
        justify-content: center;
    }

    .voir {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .voir a {
        color: white;
        z-index: 999;
        font-size: 24px;
        text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.8);
        text-decoration: none;
    }
</style>

<div class="prod-content">
    <div class="prod-back">
        <div class="prod-back-content" style="background-image: url('{{ produit.image[0] }}');"
             data-images='{{ produit.image|json_encode|raw }}' id="produit-{{ produit.id }}">
            <a class="prod-ville-name" href="{{ path('app_get_continent_produit', {continent: produit.nom}) }}">
                <strong>{{ produit.nom }}</strong>
            </a>
        </div>
    </div>

    <div class="prod-front">
        <div class="prod-img" style="background-image: url('{{ produit.image[1] }}');"></div>
        <div class="voir">
            <a href="{{ path('app_get_continent_produit', {continent: produit.nom}) }}"><strong>Découvrir</strong></a>
        </div>
    </div>
</div>
